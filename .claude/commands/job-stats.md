---
description: Show aggregate job search statistics (totals, by status, by match category, recent search runs).
---

Invoke the `job-stats` skill (see `job-search-agent/skills/job-stats/SKILL.md`).

Run `python3 job-search-agent/scripts/stats.py` and show the output to the
user, adding brief interpretation only if something stands out.
